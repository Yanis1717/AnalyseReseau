{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-5">
    {% for message in app.flashes('success') %}
        <div class="alert alert-success shadow-sm">{{ message }}</div>
    {% endfor %}

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-5 fw-bold text-primary">🛡️ Security Auditor</h1>
        <a href="{{ path('run_python') }}" class="btn btn-dark shadow-sm">
            🚀 Lancer l'Analyse (Python)
        </a>
    </div>

    <div class="card shadow-lg border-0">
        <div class="card-header bg-white border-bottom py-3">
            <h5 class="mb-0 text-muted">Rapport d'Analyse Réseau (Network_Report.md)</h5>
        </div>
        <div class="card-body bg-white p-4">
            {# On affiche le contenu du Markdown #}
            <div class="report-container" style="white-space: pre-wrap; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;">
                {{ reportContent }}
            </div>
        </div>
    </div>
    
    <div class="text-center mt-4 text-muted">
        <small>Données sources : 11,000+ paquets analysés avec succès.</small>
    </div>
</div>

<style>
    .report-container {
        background-color: #f8f9fa;
        padding: 20px;
        border-radius: 8px;
        border: 1px solid #dee2e6;
        line-height: 1.6;
        color: #212529;
    }
</style>
{% endblock %}